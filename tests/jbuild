
(unified_tests
 ((target feature-subtree-build)
  (deps (../bin/fe.exe
         (files_recursively_in bin)
         (files_recursively_in lib)
         (files_recursively_in etc)
         (files_recursively_in sample-repos)
         ../hg/hgrc
         ))
  (setup_script "bin/setup-script")))

(enforce_style ())
